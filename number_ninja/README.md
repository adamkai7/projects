To use this project numpy, random, sympy, streamlit, re, google.generativeai, math will need to be installed

To run the project locally, download the repo. Then to run it do streamlit run main.py

Thank You
